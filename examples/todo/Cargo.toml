[package]
name = "todo"
version = "5.6.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
eframe = "0.32"
catppuccin-egui = { version = "5.6", default-features = false, features = ["egui32"] }
